#include "main.h"

/**
 * _abs - calculates the absolute value of
 * an integer
 *
 * @d: input the value
 * Return: the absolute value of d
 */
int _abs(int d)
{
	if (d >= 0)
	{
		return (d);
	}
	return (-d);
}
